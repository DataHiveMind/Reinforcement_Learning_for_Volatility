╔══════════════════════════════════════════════════════════════════════════════╗
║                         FINRL QUICK REFERENCE CARD                           ║
║                    Reinforcement Learning for Volatility                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚀 INSTALLATION                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  conda env update -f environment.yml
  # or
  pip install -r requirements.txt


┌──────────────────────────────────────────────────────────────────────────────┐
│ 💡 BASIC USAGE (Python)                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

  from src.models.finrl_agent import VolatilityFinRLAgent

  # Train
  agent = VolatilityFinRLAgent(env=env, model_name="ppo")
  agent.train(total_timesteps=100000)

  # Save & Load
  agent.save("./models/my_model")
  agent.load("./models/my_model")

  # Predict
  action, _ = agent.predict(observation)


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 COMMAND LINE                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  # Single agent
  python examples/train_with_finrl.py --mode single --model ppo --timesteps 100000

  # Ensemble
  python examples/train_with_finrl.py --mode ensemble --timesteps 100000

  # With evaluation
  python examples/train_with_finrl.py --mode single --model sac --evaluate


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🤖 AVAILABLE ALGORITHMS                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

  Algorithm  │ Type        │ Best For              │ Speed
  ───────────┼─────────────┼───────────────────────┼──────
  PPO        │ On-policy   │ General, stable       │ ⚡⚡
  A2C        │ On-policy   │ Fast prototyping      │ ⚡⚡⚡
  SAC        │ Off-policy  │ Exploration           │ ⚡⚡
  DDPG       │ Off-policy  │ Deterministic         │ ⚡⚡⚡
  TD3        │ Off-policy  │ Improved DDPG         │ ⚡⚡


┌──────────────────────────────────────────────────────────────────────────────┐
│ ⚙️  HYPERPARAMETERS (PPO)                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  model_kwargs = {
      "learning_rate": 3e-4,     # [1e-5, 1e-3]
      "n_steps": 2048,           # [512, 2048, 4096]
      "batch_size": 64,          # [32, 64, 128, 256]
      "gamma": 0.99,             # [0.95, 0.999]
      "ent_coef": 0.01,          # [0.0, 0.1]
  }


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎭 ENSEMBLE METHODS                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

  from src.models.finrl_agent import FinRLEnsembleAgent

  ensemble = FinRLEnsembleAgent(env=env, model_names=["ppo", "a2c", "sac"])
  ensemble.train_all(total_timesteps=100000)

  # Different voting methods
  action = ensemble.predict(obs, method="averaging")  # Continuous
  action = ensemble.predict(obs, method="voting")     # Discrete
  action = ensemble.predict(obs, method="weighted")   # Weighted avg


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📊 MONITORING                                                                │
└──────────────────────────────────────────────────────────────────────────────┘

  # TensorBoard
  tensorboard --logdir ./tensorboard_logs/finrl --port 6006
  → http://localhost:6006

  # MLflow
  mlflow ui --port 5000
  → http://localhost:5000


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  File                               │ Purpose
  ───────────────────────────────────┼────────────────────────────────────
  FINRL_SETUP_CHECKLIST.md           │ Installation & setup steps
  docs/FINRL_QUICKSTART.md           │ 5-minute quick start
  docs/FINRL_INTEGRATION.md          │ Complete guide (518 lines)
  FINRL_INTEGRATION_SUMMARY.md       │ What was added
  notebook/06_finrl_integration.ipynb│ Interactive tutorial


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔧 TROUBLESHOOTING                                                           │
└──────────────────────────────────────────────────────────────────────────────┘

  Problem                  │ Solution
  ─────────────────────────┼────────────────────────────────────────────
  FinRL not found          │ pip install finrl
  Gym/Gymnasium error      │ pip install gymnasium>=0.28.0
  CUDA not available       │ Check: torch.cuda.is_available()
  Out of memory            │ Reduce batch_size and buffer_size


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎓 LEARNING PATH                                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  1. ✅ Read FINRL_QUICKSTART.md (5 min)
  2. ✅ Run notebook/06_finrl_integration.ipynb (30 min)
  3. ✅ Train first model: python examples/train_with_finrl.py (15 min)
  4. ✅ Compare algorithms (1 hour)
  5. ✅ Build ensemble (2 hours)
  6. ✅ Hyperparameter tuning (varies)


┌──────────────────────────────────────────────────────────────────────────────┐
│ 💾 PROJECT STRUCTURE                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  src/models/
  ├── finrl_agent.py              ← FinRL wrapper classes
  ├── ppo_agent.py                ← Your custom PPO
  └── ddpg_agent.py               ← Your custom DDPG

  examples/
  ├── train_with_finrl.py         ← Training script
  └── load_openbb_data.py

  configs/trainings/
  ├── finrl_config.yaml           ← FinRL hyperparameters
  ├── ppo_mircostructure.yaml
  └── ddpg_microstructure.yaml

  notebook/
  └── 06_finrl_integration.ipynb  ← Interactive tutorial


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🌟 KEY FEATURES                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  ✅ 5 state-of-the-art RL algorithms (PPO, A2C, SAC, DDPG, TD3)
  ✅ Ensemble methods for robust trading
  ✅ Hyperparameter optimization with Optuna
  ✅ TensorBoard & MLflow integration
  ✅ Easy save/load functionality
  ✅ Works with your existing environment
  ✅ Production-ready code
  ✅ Comprehensive documentation


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔗 RESOURCES                                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

  FinRL: https://github.com/AI4Finance-Foundation/FinRL
  Stable-Baselines3: https://stable-baselines3.readthedocs.io/
  Local docs: docs/FINRL_INTEGRATION.md


┌──────────────────────────────────────────────────────────────────────────────┐
│ ⚡ QUICK COMMANDS                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  # Test installation
  python -c "import finrl; print(f'FinRL v{finrl.__version__}')"

  # Quick train (5 min)
  python examples/train_with_finrl.py --mode single --timesteps 10000

  # Full train (30-60 min)
  python examples/train_with_finrl.py --mode single --timesteps 100000 --evaluate

  # Jupyter
  jupyter lab notebook/06_finrl_integration.ipynb

  # TensorBoard
  tensorboard --logdir ./tensorboard_logs/finrl


╔══════════════════════════════════════════════════════════════════════════════╗
║  Ready to start? Run: jupyter lab notebook/06_finrl_integration.ipynb       ║
╚══════════════════════════════════════════════════════════════════════════════╝
